# Application Configuration
module.name=trukker_wrapper
spring.application.name=Truck-Driver-WhatsApp-Wrapper
server.port=8025

spring.datasource.username=root
spring.datasource.url=jdbc:mysql://localhost:3306/trukker?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.password=r00t

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=false
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging Configuration (now handled by logback-spring.xml)
# Single log folder setup:
# - logs/application.log (all application logs from all components)
# - logs/errors.log (error-only logs for quick troubleshooting)

# Infobip API Configuration
infobip.api.url=https://meqy6.api-ae2.infobip.com
infobip.api.key=e86e7a2acf084c24eb8d5b1082bfae53-84e46d2d-d36c-4d18-b5e9-e47127f50bf4
infobip.whatsapp.from=971526328601

# TruKKer API Configuration - Removed (not implemented yet)

# Application Webhook Configuration
# app.webhook.url=http://localhost:8025/wawrapper
app.webhook.url=https://evainternal.bngrenew.com/wawrapper

# HTTP Client Configuration
spring.http.timeout.connection=30000
spring.http.timeout.read=30000

# JSON Configuration
spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jackson.serialization.write-dates-as-timestamps=false

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always

# Actuator Configuration (for health checks)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.show-extensions=true
springdoc.swagger-ui.show-common-extensions=true
springdoc.packages-to-scan=com.driver.whatsapp.wrapper.controller
springdoc.paths-to-match=/wawrapper/**
